// import { ParallelScheduleItem, ScheduleItemProps, SingleSessionScheduleItem } from "./types/schedule";

// export const isParallelSession = (
// 	item: ScheduleItemProps
//   ): item is ParallelScheduleItem => {
// 	return 'sessions' in item;
//   };

//   export const isSingleSession = (
// 	item: ScheduleItemProps
//   ): item is SingleSessionScheduleItem => {
// 	return 'event' in item && !('sessions' in item);
//   };
